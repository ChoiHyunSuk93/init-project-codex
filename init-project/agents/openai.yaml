interface:
  display_name: "Init Project"
  short_description: "Bootstrap Codex repo structure and rules"
  default_prompt: "Use $init-project to initialize or retrofit a repository with Codex AGENTS structure, root-level rule indexing, and safe GitHub workflow rules."
