interface:
  display_name: "Change Analysis"
  short_description: "Starter local skill for change analysis and scope definition"
  default_prompt: "Use $change-analysis when the work is mainly about interpreting the request, mapping impact scope, classifying the task as small/medium/large-clear/large-ambiguous, or defining acceptance criteria and risks before implementation. Prefer parallel explorer calls for independent document-analysis questions."
policy:
  allow_implicit_invocation: true
