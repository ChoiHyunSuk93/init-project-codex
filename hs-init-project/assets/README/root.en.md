# PROJECT_NAME

Short description placeholder. Replace this sentence with a one-line summary of the project.
This repository uses a project-scoped OMX install plus a team-first, Ralph-led completion model.

## Purpose

Describe what this repository is for.

## Status

This repository has been initialized with project-scoped OMX scaffolding and repository-specific rule/docs structure.
Update this README as the real project purpose, structure, and usage become concrete.

## Structure

- `AGENTS.md`: OMX-generated root contract plus a repository-specific overlay
- `.codex/`: project-scoped OMX install generated by `omx setup --scope project`
- `.omx/`: project-scoped OMX runtime state and team/Ralph artifacts
- `rule/`: authoritative repository-specific execution rules
- `docs/guide/`: user-facing workflow guides
- `docs/guide/subagent-workflow.md`: user-facing team + Ralph workflow guide
- `docs/implementation/`: user-facing final briefings after PASS

## Execution Model

Each implementation request runs through `omx team` with a leader-owned Ralph review loop.
Planning, implementation, and evaluation are mandatory minimum distinct team lanes.
Complex work may add more specialist lanes.
Internal coordination stays in `.omx/`; the durable repository output is the final briefing under `docs/implementation/`.
