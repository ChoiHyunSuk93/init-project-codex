interface:
  display_name: "HS Init Project Harness"
  short_description: "Initialize documented, evidence-based project harnesses"
  default_prompt: "Use $hs-init-project to initialize or retrofit this repository, analyze existing code when present, and complete its documentation and work records."
policy:
  allow_implicit_invocation: true
