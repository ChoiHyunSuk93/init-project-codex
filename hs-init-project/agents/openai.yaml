interface:
  display_name: "HS Init Project Harness"
  short_description: "Bootstrap a minimal Codex and Claude project contract"
  default_prompt: "Use $hs-init-project to initialize or retrofit this repository with a minimal cross-agent project contract."
policy:
  allow_implicit_invocation: true
